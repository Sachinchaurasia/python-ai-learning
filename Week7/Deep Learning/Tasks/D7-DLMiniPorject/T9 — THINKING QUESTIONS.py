# 🧠 TASK 9 — THINKING QUESTIONS

# Answer these without copying:

# What is the input to our neural network?
# What is the output?
# Why are there 3 input features?
# Why did we use Dense(8)?
# Why is the final layer Dense(1)?
# Why did we use sigmoid?
# Why did we use binary crossentropy?
# What does Adam do?
# What happens during one training step?
# What is an epoch?
# What is overfitting?
# Why do we keep test data separate?
# Why do we scale the features?
# Why do we save the scaler?
# Why do we save the trained model?